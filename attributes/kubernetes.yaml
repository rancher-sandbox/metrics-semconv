attributes:
  k8s.resource.name:
    type: string
    description: The name of the Kubernetes resource.
  k8s.resource.namespace:
    type: string
    description: The namespace of the Kubernetes resource.
  k8s.resource.gvk:
    type: string
    description: "The group, version, and kind of the Kubernetes resource."
